services:
  - type: web
    name: rookwatch
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn wsgi:app --timeout 300 --workers 1 --bind 0.0.0.0:$PORT --max-requests 200
    envVars:
      - key: GOOGLE_API_KEY
        sync: false
